# electron-builder output (directories.output in electron-builder.yml)
release/

# scratch node_modules materialized by build/beforePack.js at package time
.backend-deploy/

# Artifacts the manual checks under test/ write next to themselves
test/**/*.png
test/**/*.log
test/**/*.json
test/first-run/shots/
